name: Build and test RotateUV Native Unfold V3

on:
  workflow_dispatch:
  push:
    paths:
      - 'src/**'
      - 'tests/**'
      - 'Rotate_UV_PRO_NATIVE_UNFOLD_V2.ms'
      - 'Rotate_UV_PRO_NATIVE_UNFOLD_V2.mcr'
      - '.github/workflows/build-windows.yml'

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 30

    steps:
      - name: Checkout RotateUV repository
        uses: actions/checkout@v4

      - name: Checkout libigl 2.6.0
        uses: actions/checkout@v4
        with:
          repository: libigl/libigl
          ref: v2.6.0
          path: third_party/libigl
          fetch-depth: 1

      # Do not checkout libigl/eigen as a second GitHub repository. That
      # checkout is the failure point seen in CI. Pull the official Eigen
      # 3.4.0 release archive instead and validate the expected headers.
      - name: Fetch Eigen 3.4.0
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          New-Item -ItemType Directory -Force third_party | Out-Null
          $zip = Join-Path $env:RUNNER_TEMP 'eigen-3.4.0.zip'
          $unpack = Join-Path $env:RUNNER_TEMP 'eigen-3.4.0-unpack'
          if (Test-Path $unpack) { Remove-Item $unpack -Recurse -Force }

          curl.exe -L --fail --retry 5 --retry-delay 3 --retry-all-errors `
            -o $zip `
            'https://gitlab.com/libeigen/eigen/-/archive/3.4.0/eigen-3.4.0.zip'
          if ($LASTEXITCODE -ne 0) { throw "Eigen 3.4.0 download failed with exit code $LASTEXITCODE" }

          Expand-Archive -Path $zip -DestinationPath $unpack -Force
          $src = Join-Path $unpack 'eigen-3.4.0'
          if (-not (Test-Path (Join-Path $src 'Eigen\Core'))) {
            throw 'Eigen archive extracted, but Eigen/Core was not found.'
          }

          $dst = 'third_party\eigen'
          if (Test-Path $dst) { Remove-Item $dst -Recurse -Force }
          Move-Item $src $dst
          Write-Host 'Eigen 3.4.0 ready at third_party/eigen'

      - name: Enable MSVC x64
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Prepare output
        shell: cmd
        run: if not exist dist mkdir dist

      - name: Build Auto Seam V3 worker
        shell: cmd
        run: |
          cl /nologo /O2 /DNDEBUG /std:c++17 /EHsc /MD src\autoseam_worker.cpp /Fe:dist\RotateUV_AutoSeam.exe

      - name: Build Native Unfold V3 worker with libigl
        shell: cmd
        run: |
          cl /nologo /O2 /DNDEBUG /std:c++17 /EHsc /MD /bigobj /Ithird_party\libigl\include /Ithird_party\eigen src\unfold_worker.cpp /Fe:dist\RotateUV_Unfold.exe

      - name: Smoke test workers
        shell: pwsh
        run: |
          & 'dist\RotateUV_AutoSeam.exe'
          $seamCode = $LASTEXITCODE
          if ($seamCode -ne 2) { throw "Auto Seam worker usage test failed: $seamCode" }

          & 'dist\RotateUV_Unfold.exe'
          $unfoldCode = $LASTEXITCODE
          if ($unfoldCode -ne 2) { throw "Native Unfold worker usage test failed: $unfoldCode" }

          # Version handshake: the MaxScript refuses workers older than 3.0.0
          $seamVer = & 'dist\RotateUV_AutoSeam.exe' --version
          if ($LASTEXITCODE -ne 0) { throw "Auto Seam --version failed" }
          Write-Host "AutoSeam: $seamVer"
          if ($seamVer -notmatch 'RotateUV_AutoSeam\s+3\.') { throw "Unexpected Auto Seam version: $seamVer" }

          $unfoldVer = & 'dist\RotateUV_Unfold.exe' --version
          if ($LASTEXITCODE -ne 0) { throw "Native Unfold --version failed" }
          Write-Host "Unfold: $unfoldVer"
          if ($unfoldVer -notmatch 'RotateUV_Unfold\s+3\.') { throw "Unexpected Native Unfold version: $unfoldVer" }

          exit 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Golden-mesh regression tests
        shell: pwsh
        run: |
          python tests/make_fixtures.py
          python tests/run_tests.py dist\RotateUV_Unfold.exe
          if ($LASTEXITCODE -ne 0) { throw "Solver regression tests failed" }

      - name: End-to-end seam and unfold tests
        shell: pwsh
        run: |
          python tests/run_pipeline.py dist\RotateUV_AutoSeam.exe dist\RotateUV_Unfold.exe
          if ($LASTEXITCODE -ne 0) { throw "Pipeline regression tests failed" }

      - name: Desired-result real-model regression
        shell: pwsh
        run: |
          python tests/run_reference_regression.py dist\RotateUV_AutoSeam.exe
          if ($LASTEXITCODE -ne 0) { throw "Desired-result reference regression failed" }

      - name: Copy MaxScript and notices
        shell: pwsh
        run: |
          Copy-Item Rotate_UV_PRO_NATIVE_UNFOLD_V2.ms dist\ -Force
          Copy-Item Rotate_UV_PRO_NATIVE_UNFOLD_V2.mcr dist\ -Force
          Copy-Item README.md dist\README.md -Force
          Copy-Item CHANGES.md dist\CHANGES.md -Force
          Copy-Item THIRD_PARTY_NOTICES.md dist\THIRD_PARTY_NOTICES.md -Force

      - name: Upload Windows package
        uses: actions/upload-artifact@v4
        with:
          name: RotateUV-Native-Unfold-V3-Windows
          path: dist/*
          if-no-files-found: error
